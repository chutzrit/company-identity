# 설치 안내

에이전트가 사용자의 프로젝트에 `company-identity`를 설치할 때 따르는 절차.

## 프로젝트 설치 · 기본

1. 사용자가 열어 둔 대상 프로젝트의 절대 경로를 확인한다. 이 저장소는 대상 프로젝트 밖의 임시 폴더에 내려받고, 다운로드한 배포 폴더와 대상 프로젝트를 혼동하지 않는다.
2. 이 패키지의 `skills/company-identity/SKILL.md`를 확인한다.
3. Python 3가 있으면 내려받은 폴더의 설치 도구를 실행한다. 어느 위치에서 실행해도 되며 대상 프로젝트 안에만 파일을 쓴다.

```sh
python3 "/내려받은/폴더/scripts/install.py" --project "/대상/프로젝트"
```

실행 결과:

```text
대상 프로젝트/
├── .agents/skills/
│   ├── company-identity/                 스킬 원본 · Codex가 읽는 경로
│   └── .company-identity-install.json    설치 기록
└── .claude/skills/company-identity       원본을 가리키는 상대 심링크 · Claude Code
```

- 대상 프로젝트에 `skills/` 폴더는 만들지 않는다. 이 저장소의 `skills/company-identity`는 배포 원본의 위치일 뿐이다.
- 기본 대상은 Claude Code와 Codex다. CLI가 아직 없어도 두 경로를 준비한다.
- 이미 설치된 동일 파일은 유지한다. 이전 설치 도구로 관리한 버전은 로컬 수정이 없을 때 갱신한다.
- 기존 다른 스킬이나 사용자가 고친 파일과 충돌하면 덮어쓰지 않는다. `설치 중단:` 메시지로 위치와 달라진 파일을 알리고 오류 코드로 끝난다.
- 심링크를 만들 수 없는 환경에서는 `--copy`로 각 에이전트 경로에 복사할 수 있다. 이후에도 같은 설치 도구와 옵션으로 갱신한다.
- Python 3가 없으면 위 구조대로 에이전트의 파일 도구로 복사·연결한다. 설치를 위해 추가 런타임 설치를 강제하지 않는다.

설치 도구를 쓸 수 없는 경우에도 원본은 `.agents/skills/company-identity` 하나로 두고 `.claude/skills/company-identity`에서 상대 심링크로 연결한다. 폴더 단위 심링크를 지원하지 않으면 복사하고 그 사실을 알려준다.

### 예전 구조로 설치된 프로젝트

이전 버전은 `skills/company-identity`에 원본을 두고 두 에이전트 경로를 모두 심링크로 연결했다. 설치 도구를 같은 프로젝트에 다시 실행하면 새 구조로 옮긴다.

- 원본 폴더를 그대로 `.agents/skills/company-identity`로 옮기므로 사용자가 고친 내용은 보존된다. 설치 기록도 함께 옮기고, 비어 있는 `skills/` 폴더만 지운다.
- 로컬 수정이 있으면 구조만 옮기고 내용은 갱신하지 않는다. 이때 `설치 중단:` 메시지와 오류 코드로 끝나지만 구조 이전과 연결은 끝난 상태다. 알려준 파일을 새 배포본과 비교해, 차이가 사용자 수정뿐이면 그대로 두고 배포본도 바뀌었으면 사용자에게 확인한 뒤 반영한다.
- 추가 에이전트 경로를 연결해 두었다면 같은 `--agent-dir` 옵션으로 실행해야 그 연결도 새 원본으로 바뀐다.
- 설치 도구 없이 정리할 때도 같은 순서를 따른다. 예전 심링크는 폴더째 지우지 말고 링크만 제거한다.

## 다른 에이전트 추가

Agent Skills 지원 여부와 프로젝트 스킬 경로를 해당 에이전트 기준으로 확인한다.
확인한 경로만 추가한다. 지원 여부를 추측해 폴더를 만들지 않는다.

```sh
python3 scripts/install.py --project "/대상/프로젝트" --agent-dir ".cursor/skills"
```

추가한 경로에는 `.agents/skills/company-identity` 원본을 가리키는 상대 심링크가 생긴다.
이는 경로 지정 방법의 예시다. 추가 에이전트의 실제 인식은 별도로 확인한다.

## 전역 설치 · 사용자가 선택한 경우

스킬 로직만 전역에 설치한다. 회사 문서·설정 파일·회사 기준을 전역 지침에 넣지 않는다.

- 공통 원본: `~/.agents/skills/company-identity`
- Claude Code 연결: `~/.claude/skills/company-identity`
- Codex는 공통 원본 위치에서 읽는다.
- 에이전트마다 사용자 범위 설치 위치가 달라지면 현재 지원 경로를 확인한다.
- 기존 설치가 있으면 읽고 비교한다. 프로젝트·전역 양쪽에 중복 설치하지 않는다.

포함된 설치 도구는 프로젝트 설치용이다. 전역 설치는 위 경로에 에이전트의 파일 도구로 설치하거나 공통 skills 설치 도구의 전역 옵션을 사용한다.

## 설치 확인

- 연결된 모든 경로의 `SKILL.md`와 참조 문서를 실제로 읽을 수 있는지 확인한다.
- 원본과 연결된 파일이 같은 내용인지 확인한다.
- 가능하면 새 세션의 스킬 목록이나 이름 호출로 인식까지 확인한다.
- 파일 배치만 확인한 경우와 실제 호출까지 확인한 경우를 구분해 보고한다.

설치가 끝나면 위치와 호출 방법을 간단히 알려준다.
설치만 요청받았다면 회사 인터뷰까지 자동으로 시작하지 않는다.

## 업데이트

새 배포본의 동일 설치 도구를 같은 프로젝트에 실행한다.
회사 문서와 `.company-identity.json`은 설치 도구가 수정하지 않는다.
프로젝트에 맞춰 고친 스킬과 충돌하면 차이를 보여주고 사용자가 수정한 내용을 보존한다.
